# 🏝️ Treasure Island

This is a simple Python text adventure game where your goal is to find the hidden treasure.

The game uses basic Python features like `if/else` statements and `input()` to make choices.

## 🎮 How to Play

1. Run the Python file:

```bash
python main.py
